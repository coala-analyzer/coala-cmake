# TODO put here "add_coala_target" that generates a target for coala
#      TODO Also double signature like "execute_coala", but creates a target.
function(add_coala_target NAME)
    add_custom_target(${NAME})
endfunction()
