# Include the cmake files containing the macros and other stuff. Especially
# needed to enable the `FindCoala` module for `find_package`.
LIST(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}/modules")
